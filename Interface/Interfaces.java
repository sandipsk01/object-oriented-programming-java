package Interface;
// Interface is pure abstract
// Methods in interface are public and abstract by default 
// Variables in interface are public final and static by default
public interface Interfaces {
		double PI= 3.14;
		boolean isMotorized();
		String getCompany();
		//int numGears();
}
